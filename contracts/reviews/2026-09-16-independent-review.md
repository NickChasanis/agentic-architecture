# Independent review — CONTRACT-001 draft specifications

- Reviewer: `/root/contract_review` (grant REVIEW-CONTRACT-001, board event 65).
- Reviewed baseline: `9838d794423c444908455e3b33a53a1cf16efde1`; working tree HEAD `8b9db72` (grant commit) differs only in `coordination/BOARD.md` (event 65 record); tree clean at review time.
- Reviewed at: 2026-09-16T18:48Z UTC (environment clock). Packet revision 14, board revision 56, assignment generation 1.
- Scope reviewed: `contracts/README.md`, `contracts/commerce-publication.md`, `contracts/work-submission.md`, `contracts/acceptance-scenarios.md`, `contracts/commerce-http-api.md`, `contracts/identity-and-access.md`, `pilot/contracts/README.md`, `pilot/contracts/fastify/README.md`, `coordination/tasks/CONTRACT-001.md`, plus primary artifacts needed to verify those claims (`pilot/contracts/operations/http.json`, `pilot/contracts/schemas/wire.schema.json`, `pilot/contracts/check_contracts.py`, `pilot/contracts/fastify/harness.test.mjs`, `pilot/README.md`, PUBLICATION-001/STACK-001/SETTING-001/IDENTITY-001 decisions, `coordination/templates/message.md`).
- Verification performed in this review: executed the documented offline checker (`PYTHONDONTWRITEBYTECODE=1 python3 pilot/contracts/check_contracts.py`) → `PASS: 31 schema definitions, 15 operation mappings, 88 payload cases`; counted schema definitions (31) and manifest operations (15) directly; inspected harness source and manifest/schema/decision content; programmatically verified every relative Markdown link in the eight scoped files resolves. No Node/npm execution (would install into `pilot/node_modules`, outside the read-only grant), no services, no containers, no paid calls.

## 1. Verdict

**CONDITIONALLY ACCEPTED** — all six acceptance obligations are specified and verifiable, but the drafts are accepted only after the recorded consistency corrections are applied and the independent-review evidence recorded, because CON-06 (review before dependent implementation) was only partially satisfied in sequencing and several artifacts report stale check counts (27/13/74) that contradict the executable artifacts at the same baseline (31/15/88).

## 2. Per-obligation findings

| Obligation | Verdict | Evidence (file:location) | Required change |
|---|---|---|---|
| CON-01 — name contract versions, providers, consumers, decision owners | met | Version headers: `contracts/commerce-publication.md:1` (0.1-draft), `contracts/work-submission.md:1` (0.1-draft), `contracts/identity-and-access.md:1` (0.1-draft), `contracts/commerce-http-api.md:1` (0.2-draft), `contracts/acceptance-scenarios.md:1`, `pilot/contracts/README.md:1` and manifest `manifestVersion` (`operations/http.json:2`). Providers/consumers: `commerce-publication.md:3`, `work-submission.md:3`, `identity-and-access.md:3`. Decision owners: owner decisions `PUBLICATION-001-immutable-pilot.md` (accepted), `SETTING-001`, `STACK-001`, `IDENTITY-001`; proposed steward (coordinator) named in `contracts/README.md:12` and each draft. | None for the draft; note that versions are draft labels pending owner acceptance, and the index (`contracts/README.md`) does not tabulate the per-artifact version labels — add an index version list when freezing. |
| CON-02 — inputs, observable outcomes, invalid/denied cases, repeated/stale semantics | met | Observable outcomes + preconditions: `commerce-publication.md:30-38`. Error categories and non-disclosure rules: `commerce-publication.md:41-43`. Inputs/payloads: `commerce-http-api.md:35-49`. Per-operation outcomes incl. 404 concealment: `commerce-http-api.md:55-67`. Error envelope/codes/precedence: `commerce-http-api.md:69-99`. Repeated/stale: idempotent publish and non-idempotent creates `commerce-publication.md:39`; retry/uncertain-response `commerce-publication.md:43`, `commerce-http-api.md:107-109`; last-committed draft replacement `commerce-http-api.md:103-105`; in-flight reads `commerce-publication.md:47`; expiry/revocation semantics `identity-and-access.md:50,65`. | None. |
| CON-03 — assignment generation, task version, ownership, evidence checks, rejection of obsolete work | met | Envelope fields incl. `assignment_generation`, `observed_task_record_version`, `packet_revision`, `contract_versions`, `source_baseline_revision`, `check_records`: `work-submission.md:15-25`. Generation/task-version semantics: `work-submission.md:9-11`. Acceptance/rejection incl. dedup, record-change revalidation, generation rejection after reassignment, unrun-checks-as-blocker: `work-submission.md:31-35`, `:45`. Aligns with runbook `coordination/README.md:32-35` and template `coordination/templates/message.md`. WORK-01..11 scenarios: `acceptance-scenarios.md:41-51`. | None. |
| CON-04 — map obligations to planned executable checks; identify unavailable environments without inventing runnable commands | met (with correction) | PUB-01..10 `acceptance-scenarios.md:9-19`; AUTH-01..08 `:28-35`; WORK-01..11 `:41-51`; CON-01..06 coverage table `:61-68`. Operation→obligation mapping in machine manifest `operations/http.json` (each operation carries `obligations`). Offline command real and reproduced here: `pilot/contracts/README.md:30-33` → PASS 31/15/88 (see finding C1). Unavailable environments explicitly labelled not-run: `acceptance-scenarios.md:3`, `pilot/contracts/README.md:44`, `pilot/contracts/fastify/README.md:36-40` (proof boundaries) — no runnable commands invented for absent environments. | Update stale results in `pilot/contracts/README.md:36-40`, `pilot/contracts/fastify/README.md:17`, and `coordination/tasks/CONTRACT-001.md:34` to the current checker output (31/15/88). |
| CON-05 — product extension and worker-adapter replacement expectations, supported compatibility combinations, change-surface budgets | met | Second-consumer expectations + zero-change budget: `commerce-publication.md:51-55` (zero existing provider-module/original-storefront changes; new consumer code/registration allowed; finding-not-weakening rule). Adapter replacement + zero-coordinator-logic budget: `work-submission.md:47-51` (capability declaration, same conformance checks, manual-rehearsal limit). Supported combinations matrix: `acceptance-scenarios.md:20` (catalog 0.1-draft + merchant admin + storefront first; add second listing for PUB-10; unsupported versions explicit, draft labels non-authorizing). | None; budgets remain draft acceptance criteria (already labelled as such). |
| CON-06 — assign shared-contract ownership and obtain review before dependent implementation | partially met | Ownership assigned: coordinator named steward for both contracts everywhere (`contracts/README.md:12`, `commerce-publication.md:3`, `work-submission.md:3`). Review: only a coordinator self-review existed before implementation (`coordination/tasks/CONTRACT-001.md:38`; the self-review was **not** used here). Dependent implementation (identity/shop foundation, catalog publication, merchant list, pagination) was built and integrated under the still-draft contracts — see `pilot/README.md:18-20` and board events 18–30 — i.e., independent review was **not** obtained before dependent implementation, contrary to the literal obligation. The drafts themselves repeatedly and honestly gate dispatch on independent review/no non-dispatchable status (`pilot/contracts/README.md:3,48`; `operations/http.json:3`). This review is the independent review, now recorded. | Record this review as the CON-06 review evidence; establish/confirm a gating rule that further dependent implementation proceeds only against the owner-accepted contract baseline, and record the accepted final baseline. |

## 3. Consistency findings across artifacts

- **C1 (confirmed, required fix — documented counts contradict the executable artifacts at the same baseline).** `pilot/contracts/README.md:5-8,36-40` ("27 … 13 … 74"), `pilot/contracts/fastify/README.md:17` ("all 13 operation mappings; 74 Python payload cases"), and `coordination/tasks/CONTRACT-001.md:34` ("27 draft definitions, 13 HTTP operations, and 74 passing offline payload cases") all quote counts the current checker does not produce. Executing the documented command in this review yields `PASS: 31 schema definitions, 15 operation mappings, 88 payload cases`; the manifest contains HTTP-01..09 and ID-01..06 (15 operations, `operations/http.json:34-407`) and the schema bundle has 31 definitions. The 13/74 figures are stale (the merchant-list HTTP-08/HTTP-09 and page schemas were added later), and the Fastify "table-driven success check for all 13 operation mappings" is also wrong because `harness.test.mjs:116-132` iterates every `manifest.operations` entry.
- **C2 (minor).** Version labels are not reconciled across layers: HTTP spec `0.2-draft` (`commerce-http-api.md:1`) vs machine manifest `manifestVersion: "0.1-draft"` (`operations/http.json:2`); identity 0.1-draft. Factual content is consistent, and the index does not tabulate versions. Fix with C1 or at freeze.
- **C3 (minor/stale).** `acceptance-scenarios.md:3` ("there is no application or second adapter yet") contradicts the connected pilot (`pilot/README.md:18-20`) and that file's own later merchant-list sections referencing HTTP-08/09 (`acceptance-scenarios.md:70-91`). The earlier no-runtime statement was not updated when the pilot and the MC/PAGE obligations were added.
- **C4 (consistent).** Error-code/status mapping and error-envelope precedence agree between `commerce-http-api.md:85-97`, `identity-and-access.md:73` (identity extensions incl. `LOGIN_RESPONSE_INVALID`/`LOGIN_FAILED`/`ACCOUNT_UNAVAILABLE`/`IDENTITY_SERVICE_UNAVAILABLE`), and `operations/http.json:17-33`. Operation→obligation IDs (PUB/AUTH/MC/PAGE) agree between the manifest and `acceptance-scenarios.md`; HTTP-01/02/05/06/07→PUB-01, HTTP-03/04→reload/PUB-08, HTTP-06/07→PUB-02/07/10 tracing in `commerce-http-api.md:136-138` matches the manifest.
- **C5 (consistent).** Repeated/stale-action semantics agree across artifacts: publish idempotent + uncertain-transport retry (`commerce-publication.md:43`, `commerce-http-api.md:107-109`, HTTP-05/PUB-05,06,09 in manifest); non-idempotent creates (`commerce-http-api.md:109`); last-committed draft updates (`contracts/README.md:19`, `commerce-http-api.md:105`); in-flight reads may see old state (`commerce-publication.md:47`); authorization recheck-not-freeze and post-commit revocation denial (`identity-and-access.md:50`); 404 concealment for inaccessible resources (`commerce-http-api.md:53,63`, identity processing order `identity-and-access.md:75`).
- **C6 (consistent).** Kinds/responses of ID-01 (302 to provider) and ID-02 (303 to fixed landing, query shape permitting single code/error extension) match between `identity-and-access.md:58-59`, the manifest, and the callback caveat in `pilot/contracts/README.md:21`.
- **C7 (verified).** All relative Markdown cross-references in the eight scoped files resolve to files that exist in this tree (checked programmatically).

## 4. Independent-review honesty note

I did not open, reuse, or rely on the coordinator's prior self-review `contracts/reviews/2026-09-12-foundation-readiness.md`. It is referenced by scoped documents but was not read; every finding above derives from the CONTRACT-001 packet and the scoped contract artifacts, the machine-readable manifest/schema/checker/harness sources, the recorded owner decisions, and the offline check executed in this session. The count discrepancy (C1) was discovered by running the documented checker, not from any prior review.