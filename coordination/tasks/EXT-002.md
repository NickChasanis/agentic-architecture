# EXT-002 — Fresh notes consumer contract

Packet revision: 1. This is a fresh comparable task, not a grant. Canonical authority is `/root` in the originating checkout; copied packets and GitHub snapshots do not grant work.

Implement `selectByTag(notes, tag)` in `examples/portable-context/src/select-by-tag.mjs`. Return active notes whose tags contain the exact string, preserving order and object identity. Archived notes never return; empty tags match `''`; non-string tags and every malformed or duplicate-ID record throw; inputs remain unchanged.

Consume the record shape in `examples/portable-context/README.md`, but do not modify `filter.mjs`, `tags.mjs`, this evaluator or shared contracts. The fresh oracle is `examples/portable-context/fresh-task.contract.test.mjs`. Prior notes implementation is not a cohort artifact.

Required command: `node --test examples/portable-context/fresh-task.contract.test.mjs`. Acceptance additionally requires coordinator path/contract/revision review and standard offline verification. No database, browser, production or paid API resources.

Freeze this packet, evaluator and context hash before any model assignment. Record provider/model identity, retries, failures, review/integration effort, UTC times and cost; unavailable values remain null. Worker owns only `examples/portable-context/src/select-by-tag.mjs`; coordinator owns evaluator, packet, board and integration. Use a clean worktree and revalidate before edit, resume and submission. Budget: 90 active implementation minutes, 15-minute checkpoints, 30-minute review, no paid API.
