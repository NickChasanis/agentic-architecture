# OPS-001 — Supervised operational readiness

Packet revision: 1. Authority: canonical `coordination/BOARD.md` in the originating checkout, coordinator `/root`.

Owner authorized the six phases on 2026-09-14. Baseline: `3f13c5b5c70b869cf73b47ce3f2f9c46759915b2`.

Coordinator scope: baseline fixes and reproducible verification; recovery/backup exercises; comparison protocol; portable example; CI and readiness documentation. Required context: README, coordination runbook/board, chapter 20, authority policy and existing adapter/registry contracts. Keep Markdown as live authority. No production deployment or separate paid model calls. Existing host services remain untouched.

WORKER-OPS-001 subtask, generation/packet/record 1: implement a supervised Git worker execution bridge, isolated worktree `/home/nchasanis/.config/superpowers/worktrees/agentic-architecture/worker-ops`, branch `implementation/worker-ops`, baseline above. Worker `/root/worker_ops` owns only `pilot/contracts/coordination/git-worker.mjs`, `git-worker.test.mjs`, and `adapter.mjs`. No shared docs, board, dependencies, DB or browser resources. Budget 90 active minutes, 15-minute progress checks, 30-minute coordinator review; no separately billed calls. Native agent transport is coordinator-operated; a command process is not automatically a model benchmark.

Obligations: GW-01 supplied path is an actual clean Git worktree at the grant baseline, canonical checkout excluded, branch attached; GW-02 argv executes in assigned cwd and bounded output is collected; GW-03 completed artifact is a descendant commit, actual changed paths satisfy grant ownership, dirty/untracked files prevent acceptance; GW-04 nonzero exit, missing commit, stale baseline and outside-scope edits fail closed; GW-05 status/cancel uses existing adapter contract, current grant revalidated before launch and submission. Use injected authority callback for latest grant; no copied board authority. Tests create disposable Git repos with real child processes; do not invoke paid models. Preserve rejected artifacts. A result is accepted for coordinator review only, never automatically integrated.

Submission: commit assigned paths, rerun tests at commit, report exact revision, UTC check times, commands, test results and limitations through native message. Do not update the board. Checkpoint before resumption or submission.
