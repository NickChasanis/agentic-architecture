# TEMPLATE — Task packet (not an assignment)

```text
task_id:
packet_revision:
canonical_board_location: confirm with coordinator; never assume a copied board is live
outcome and non_goals:
acceptance_obligation_ids and observable scenarios:
required_context:
provided/consumed contract versions:
prerequisite task IDs and accepted artifact revisions:
verification commands and environment, or discussion-review criteria:
expected deliverable and handoff evidence:
open questions:
```

The coordinator records status, owner, generation, baseline, workspace, write scope, timestamps, and budget in the board when granting work. The packet must not maintain an independent competing status. Unresolved readiness fields prevent dispatch. Create real packets under `tasks/`; do not claim this template.
