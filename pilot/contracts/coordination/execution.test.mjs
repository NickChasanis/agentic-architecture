import {test} from 'node:test';
import assert from 'node:assert/strict';
import {CoordinationRegistry} from './registry.mjs';
test('execution envelope enters review only after matching evidence',()=>{const rev='b'.repeat(40);const r=new CoordinationRegistry();r.grant({taskId:'E-1',workerId:'w',generation:1,recordVersion:1,packetRevision:1,contracts:{x:'1'},baseline:rev,ownedPaths:['pilot/'],requiredObligations:['X'],state:'running',budget:{maxMinutes:1,billing:'no-paid-api'},checkpointMinutes:1,testResources:['node']});const s={submissionId:'S',taskId:'E-1',workerId:'w',generation:1,recordVersion:1,packetRevision:1,contracts:{x:'1'},baseline:rev,artifactRevision:rev,artifactLocation:'git:'+rev,changedPaths:['pilot/a'],checks:[{obligation:'X',command:'test',environment:'local',testedRevision:rev,startedAt:'2026-09-12T00:00:00Z',endedAt:'2026-09-12T00:01:00Z',outcome:'pass',evidenceLocation:'e'}],unrunChecks:[],limitations:[]};assert.equal(r.submit(s).status,'accepted-for-review');});
