# Pilot evaluation

The evaluator measures mandatory-obligation coverage, conformance, extension change surface, breaking changes, elapsed minutes and available tool cost. Missing cost data is reported as `unavailable`; it is never estimated. A single pilot is qualitative evidence, not a speedup benchmark.
