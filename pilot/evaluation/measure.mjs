export function measure(e){const required=new Set(e.required);const passed=new Set((e.checks??[]).filter(c=>c.outcome==='pass').map(c=>c.obligation));const coverage=required.size?[...required].filter(x=>passed.has(x)).length/required.size:1;const outside=(e.changedPaths??[]).filter(p=>!(e.declaredPaths??[]).some(root=>p===root||p.startsWith(root+'/'))).length;return {coverage,conformance:coverage===1?'pass':'fail',outsideBoundary:outside,breakingChanges:e.breakingChanges,cost:e.cost==null?'unavailable':e.cost,minutes:e.minutes};}
